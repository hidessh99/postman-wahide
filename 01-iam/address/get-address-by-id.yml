info:
  name: get-address-by-id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/address/ADDRESS_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
