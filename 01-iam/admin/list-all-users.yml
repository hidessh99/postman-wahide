info:
  name: list-all-users
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/user"
  params:
    - name: page
      value: "1"
      type: query
    - name: page_size
      value: "20"
      type: query
    - name: search
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
