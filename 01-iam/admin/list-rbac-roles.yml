info:
  name: list-rbac-roles
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/role"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
