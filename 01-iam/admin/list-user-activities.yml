info:
  name: list-user-activities
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/user-activity"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
