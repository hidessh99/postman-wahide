info:
  name: list-user-login-logs
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/user-log"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
