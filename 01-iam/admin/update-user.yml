info:
  name: update-user
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/admin/user/USER_ID"
  body:
    type: json
    data: |-
      {
        "name": "Updated User Name",
        "email": "updated@example.com",
        "phone_number": "6281234567890",
        "is_active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
