info:
  name: generate-apikey-token
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
