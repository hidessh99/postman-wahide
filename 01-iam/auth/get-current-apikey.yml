info:
  name: get-current-apikey
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
