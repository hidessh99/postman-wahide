info:
  name: revoke-apikey
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/auth/token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
