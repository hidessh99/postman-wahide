info:
  name: get-user-dashboard-stats
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/dashboard/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
