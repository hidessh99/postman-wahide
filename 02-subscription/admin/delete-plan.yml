info:
  name: delete-plan
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/admin/plans/PLAN_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
