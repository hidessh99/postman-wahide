info:
  name: create-device-slot
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/wa/devices"
  body:
    type: json
    data: |-
      {
        "push_name": "Wahide Customer Service 1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
