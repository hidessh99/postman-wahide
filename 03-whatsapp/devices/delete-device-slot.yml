info:
  name: delete-device-slot
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/wa/devices/DEVICE_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
