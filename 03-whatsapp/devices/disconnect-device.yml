info:
  name: disconnect-device
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/wa/devices/DEVICE_ID/disconnect"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
