info:
  name: list-tenant-devices
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/wa/devices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
