info:
  name: pair-device-qr-code
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/wa/devices/DEVICE_ID/pair"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
