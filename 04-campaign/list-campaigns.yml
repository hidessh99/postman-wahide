info:
  name: list-campaigns
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/campaigns"
  params:
    - name: page
      value: "1"
      type: query
    - name: page_size
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
