info:
  name: start-campaign-dispatch
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/campaigns/CAMPAIGN_ID/start"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
