info:
  name: list-contacts
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/contacts"
  params:
    - name: page
      value: "1"
      type: query
    - name: page_size
      value: "50"
      type: query
    - name: tag_id
      value: ""
      type: query
    - name: search
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
