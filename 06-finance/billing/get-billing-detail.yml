info:
  name: get-billing-detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/billing/BILLING_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
