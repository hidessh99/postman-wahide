info:
  name: get-invoice-detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/invoice/INVOICE_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
