info:
  name: get-accumulative-report
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/report/always"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
