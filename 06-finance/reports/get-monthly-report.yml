info:
  name: get-monthly-report
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/report/month"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
