info:
  name: get-post-by-slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/posts/panduan-whatsapp-gateway"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
