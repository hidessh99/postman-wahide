info:
  name: close-ticket
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/ticket/TICKET_ID/close"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
