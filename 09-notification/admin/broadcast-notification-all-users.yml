info:
  name: broadcast-notification-all-users
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/broadcast/all"
  body:
    type: json
    data: |-
      {
        "title": "Pemeliharaan Sistem Terjadwal",
        "message": "Server akan melakukan maintenance rutin pada Minggu dini hari pukul 01:00 WIB.",
        "type": "SYSTEM_ALERT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
