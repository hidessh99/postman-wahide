info:
  name: list-notification-queues
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/queue"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
