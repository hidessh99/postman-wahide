info:
  name: 02-subscription-check-expired
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/subscription/check-expired"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
