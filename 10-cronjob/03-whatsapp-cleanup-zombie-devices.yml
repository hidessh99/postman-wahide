info:
  name: 03-whatsapp-cleanup-zombie-devices
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/whatsapp/cleanup-devices"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
