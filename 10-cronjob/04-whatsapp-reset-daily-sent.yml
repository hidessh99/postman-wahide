info:
  name: 04-whatsapp-reset-daily-sent
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/whatsapp/reset-daily-sent"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
