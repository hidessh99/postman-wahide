info:
  name: 06-notification-cleanup-queues
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/cleanup/queues"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
