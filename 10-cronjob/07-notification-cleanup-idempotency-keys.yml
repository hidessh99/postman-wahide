info:
  name: 07-notification-cleanup-idempotency-keys
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/cleanup/idempotency-keys"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
