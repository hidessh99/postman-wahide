info:
  name: 09-iam-cleanup-user-logs
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/cleanup/user-logs"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
