info:
  name: 11-content-cleanup-storage-r2
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/cleanup/storage-r2"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
