info:
  name: 13-finance-cleanup-invoices
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/cleanup/invoices"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
