info:
  name: 14-finance-cleanup-income-pending
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/cronjob/cleanup/income-pending"
  headers:
    - name: X-Cron-Secret
      value: "{{cronSecret}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
